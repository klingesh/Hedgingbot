Drop trades_export.json here.

Produced by tools/export_tradingbot_trades.py, which must be COPIED INTO and
RUN FROM the Tradingbot repo (it imports Tradingbot's engine and strategies).

This directory is gitignored for everything except this note: exported trade
files are derived data, can be tens of MB, and are reproducible from Tradingbot
at any time.
